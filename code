If message.payment.type in ('DEVICE_REG') then do;
	profile.customerxdevice.H_REG_DT=message.payment.createdDt;
end;

if message.payment.type in ('LOGIN') then do;
	if missing(profile.customerxdevice.H_REG_DT) then do;
	    profile.customerxdevice.H_REG_DT=message.payment.createdDt;
    end;
end;
